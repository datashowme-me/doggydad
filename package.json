{
  "name": "doggydad-blog",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=doggydad",
    "deploy:cloudflare": "npm run deploy",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
